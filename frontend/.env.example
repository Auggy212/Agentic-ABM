# Optional: set this to a full backend origin like http://localhost:3001.
# Leave blank in local dev to use Vite's built-in /api proxy.
VITE_API_BASE_URL=
# Set to "true" to route API calls through MSW mocks (no backend required)
VITE_USE_MOCKS=false
