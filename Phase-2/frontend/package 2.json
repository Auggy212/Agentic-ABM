{
  "name": "phase-1-frontend",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ../backend/frontend run dev",
    "build": "npm --prefix ../backend/frontend run build",
    "lint": "npm --prefix ../backend/frontend run lint",
    "preview": "npm --prefix ../backend/frontend run preview"
  }
}
